import 'package:flutter/material.dart';

class Awalan extends StatelessWidget {
  const Awalan({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset("assets.bromoo.jpg"),
    );
  }
}